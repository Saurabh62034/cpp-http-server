# C++ HTTP Server

Learning project to build a production-grade HTTP server from scratch.

## Features

- TCP sockets
- HTTP parsing
- Thread pool
- Logging
- Routing
- REST APIs

## Build

```bash
mkdir build
cd build
cmake ..
make